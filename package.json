{
  "name": "ztools-native-api",
  "version": "1.0.0",
  "description": "Cross-platform native APIs for Node.js (macOS: Swift + N-API, Windows: Win32 + N-API)",
  "main": "index.js",
  "scripts": {
    "build": "node scripts/build.js",
    "build:swift": "sh scripts/build-swift.sh",
    "clean": "node-gyp clean && node scripts/clean.js",
    "test": "node test/test-all.js",
    "install": "npm run build"
  },
  "keywords": [
    "macos",
    "windows",
    "native",
    "clipboard",
    "window-manager",
    "swift",
    "win32",
    "napi"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-addon-api": "^8.0.0"
  },
  "devDependencies": {
    "node-gyp": "^10.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
